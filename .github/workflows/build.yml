name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            name: macOS ARM64
          - platform: macos-latest
            target: x86_64-apple-darwin
            name: macOS x64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            name: Windows x64

    runs-on: ${{ matrix.platform }}
    name: Build ${{ matrix.name }}

    steps:
      # -------------------------
      # 基础环境
      # -------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # -------------------------
      # 前端依赖
      # -------------------------
      - name: Install frontend dependencies
        run: pnpm install

      # -------------------------
      # 构建 + 发布 Release
      # -------------------------
      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'WLZJ Match Downloader ${{ github.ref_name }}'
          releaseDraft: false
          prerelease: false
          args: --target ${{ matrix.target }}

      # -------------------------
      # 🔐 生成 .sig 签名文件（用于自动更新）
      # -------------------------
      - name: Install Tauri CLI for signing
        if: matrix.platform != 'ubuntu-22.04'
        run: cargo install tauri-cli --version "^2.0.0"

      - name: Generate .sig signature files
        if: matrix.platform != 'ubuntu-22.04'
        shell: bash
        run: |
          set -e
          
          echo "🔍 Looking for installer files..."
          
          TARGET="${{ matrix.target }}"
          BUNDLE_DIR="src-tauri/target/${TARGET}/release/bundle"
          
          # 根据平台查找对应的安装包
          if [[ "${{ matrix.platform }}" == "macos-latest" ]]; then
            PATTERN="*.dmg"
          elif [[ "${{ matrix.platform }}" == "windows-latest" ]]; then
            PATTERN="*.msi"
          fi
          
          echo "📂 Searching in: $BUNDLE_DIR"
          echo "🔎 Pattern: $PATTERN"
          
          # 查找所有匹配的文件
          find "$BUNDLE_DIR" -type f -name "$PATTERN" | while read -r INSTALLER; do
            echo ""
            echo "📦 Found: $INSTALLER"
            
            # 检查是否已经有 .sig 文件
            if [ -f "${INSTALLER}.sig" ]; then
              echo "✅ Signature already exists: ${INSTALLER}.sig"
            else
              echo "🔏 Generating signature..."
              
              # 生成签名 - 根据密码是否为空决定是否传递 --password 参数
              PRIVATE_KEY="${{ secrets.TAURI_PRIVATE_KEY }}"
              PASSWORD="${{ secrets.TAURI_KEY_PASSWORD }}"
              
              if [ -z "$PASSWORD" ]; then
                # 密码为空，不传递 --password 参数
                echo "📝 Using key without password"
                cargo tauri signer sign \
                  "$INSTALLER" \
                  --private-key "$PRIVATE_KEY"
              else
                # 密码不为空，传递 --password 参数
                echo "📝 Using key with password"
                cargo tauri signer sign \
                  "$INSTALLER" \
                  --private-key "$PRIVATE_KEY" \
                  --password "$PASSWORD"
              fi
              
              if [ -f "${INSTALLER}.sig" ]; then
                echo "✅ Signature created: ${INSTALLER}.sig"
              else
                echo "❌ Failed to create signature"
                exit 1
              fi
            fi
          done
          
          echo ""
          echo "📋 All files in bundle directory:"
          find "$BUNDLE_DIR" -type f

      # -------------------------
      # 上传构建产物（可选，但推荐）
      # -------------------------
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}-artifacts
          path: src-tauri/target/${{ matrix.target }}/release/bundle/**/*
          retention-days: 7
